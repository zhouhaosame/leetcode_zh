bool isMatch(string s, string p)
    {		int i = 0, j = 0, n = s.size(), m = p.size();		bool **record = new bool*[m + 1];		for (int u = 0; u < m + 1; u++)		{			record[u] = new bool[n + 1];                        memset(record[u], 0, sizeof(bool)*(n+1));//设置初值为0		}		record[0][0] = true;		for (int i = 1; i <= m; i++)//初始化矩阵，对于存在'*'为字符串首的情况			record[i][0] = record[i - 1][0] && p[i - 1] == '*';		for (j = 0; j<m; j++)		{			for (i = 0; i < n; i++)//3种情况判定			{				if (s[i] == p[j] || p[j] == '?')				{					record[j + 1][i + 1] = record[j][i];				}				else if (p[j] == '*')				{					record[j + 1][i + 1] = record[j][i + 1] || record[j + 1][i];				}				else				{					record[j + 1][i + 1] = 0;				}			}		}		return record[m][n];	}};
